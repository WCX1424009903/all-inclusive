spring:
  application:
    name: gateway-config
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:8848
        #是否注册服务到配置中心
        register-enabled: true
  profiles:
    active: dev